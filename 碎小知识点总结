*整数相除只能得到整数；

int x = 3;
int y = 4;
System.out.printIn(x/y);  //结果为0
System.out.printIn(x*1.0/y);  //结果为0.75
×1.0后变成了浮点数

将a,b的值互换
1.通过第三方temp转接
2.通过异或
  a=a^b;
  b=a^b;
  a=a^b;
3.通过加减法
  a=a+b;
  b=a-b;
  a=a-b;
  
  
  s+=1
  不等价于 s= s + 1;
  而是等价于 s = (s的数据类型)（s+1）;
  
  &&和&
  A:最终结果一样;
  B:&&具有短路作用；
  只要第一个为False，则第二个不执行;
